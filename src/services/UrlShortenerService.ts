import { BadRequestError } from "../exceptions";

/**
 * @class UrlShortenerService
 */
class UrlShortenerService {

    static async createShortUrl(longUrl: string): Promise<void> {
        
    }

}

export default UrlShortenerService;